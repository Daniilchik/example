diff --git a/helm-charts/ozone/templates/_helpers.tpl b/helm-charts/ozone/templates/_helpers.tpl
index b525c59..1f6e5e0 100644
--- a/helm-charts/ozone/templates/_helpers.tpl
+++ b/helm-charts/ozone/templates/_helpers.tpl
@@ -213,6 +213,9 @@ Usage: {{- include "ozone.krb5Volume" . | nindent 8 }}
 {{- if and .Values.istio.enabled .Values.istio.injection.enabled }}
 sidecar.istio.io/inject: "true"
 proxy.istio.io/config: '{"holdApplicationUntilProxyStarts": true}'
+{{- if and .Values.fluentBit.enabled .Values.fluentBit.restlogsPort }}
+traffic.sidecar.istio.io/excludeOutboundPorts: {{ .Values.fluentBit.restlogsPort | quote }}
+{{- end }}
 {{- end }}
 {{- end -}}
 
@@ -242,3 +245,105 @@ Usage: {{- include "ozone.secmanLabels" . | nindent 8 }}
 secman-injector: enabled
 {{- end }}
 {{- end -}}
+
+{{/*
+Fluent-bit sidecar container.
+Tails the component's tech log and (optionally) audit log from the shared
+emptyDir volume and ships them to the external collector.
+
+Args (dict):
+  ctx        — root context ($)
+  component  — component name used as `component` label on log events (om, scm, datanode, s3g, recon)
+  techPath   — absolute path of the tech log file to tail (e.g. /var/log/ozone/om.log)
+  auditPath  — absolute path of the audit log file to tail; pass "" if the component has no audit log
+
+Usage:
+  {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "om" "techPath" "/var/log/ozone/om.log" "auditPath" "/var/log/ozone/om-audit.log") | nindent 8 }}
+*/}}
+{{- define "ozone.fluentbit.container" -}}
+{{- $ctx := .ctx -}}
+- name: fluent-bit
+  image: {{ $ctx.Values.fluentBit.image }}
+  imagePullPolicy: {{ $ctx.Values.fluentBit.imagePullPolicy | default "IfNotPresent" }}
+  env:
+    - name: FLUENTBIT_COMPONENT
+      value: {{ .component | quote }}
+    - name: FLUENTBIT_TECH_LOG_PATH
+      value: {{ .techPath | quote }}
+    - name: FLUENTBIT_AUDIT_LOG_PATH
+      value: {{ .auditPath | default "/var/log/ozone/.no-audit" | quote }}
+    - name: CLUSTER_NAME
+      value: {{ $ctx.Values.fluentBit.clusterName | quote }}
+    - name: POD_NAMESPACE
+      value: {{ include "ozone.namespace" $ctx | quote }}
+  ports:
+    - containerPort: {{ $ctx.Values.fluentBit.fluentMetricsPort }}
+      name: fb-metrics
+      protocol: {{ $ctx.Values.fluentBit.fluentMetricsProtocol | default "TCP" }}
+  {{- include "ozone.containerSecurityContext" $ctx | nindent 2 }}
+  {{- with $ctx.Values.fluentBit.resources }}
+  resources:
+    {{- toYaml . | nindent 4 }}
+  {{- end }}
+  volumeMounts:
+    - name: fluent-bit-config
+      mountPath: /fluent-bit/etc/
+    - name: fluent-bit-tmp
+      mountPath: /tmp
+    - name: ozone-logs
+      mountPath: /var/log/ozone
+{{- end -}}
+
+{{/*
+Volumes required by fluent-bit sidecar + the shared log dir.
+Usage:
+  {{- include "ozone.fluentbit.volumes" $ | nindent 8 }}
+*/}}
+{{- define "ozone.fluentbit.volumes" -}}
+- name: fluent-bit-config
+  configMap:
+    name: fluent-bit-ozone-config
+- name: fluent-bit-tmp
+  emptyDir: {}
+- name: ozone-logs
+  emptyDir: {}
+{{- end -}}
+
+{{/*
+VolumeMount for the main container, pointing at the shared log dir.
+The main Ozone container must write to /var/log/ozone so fluent-bit
+can tail those files from its own mount of the same emptyDir.
+Usage:
+  {{- include "ozone.fluentbit.logsMount" $ | nindent 12 }}
+*/}}
+{{- define "ozone.fluentbit.logsMount" -}}
+- name: ozone-logs
+  mountPath: /var/log/ozone
+{{- end -}}
+
+{{/*
+Idle cli sidecar — lets operators exec into the pod for debugging.
+Mounts the shared log dir (when fluent-bit is enabled) and the config map
+so you can inspect /var/log/ozone/*.log and /ozone/etc/hadoop/* from a shell.
+Usage:
+  {{- include "ozone.cli.container" . | nindent 8 }}
+*/}}
+{{- define "ozone.cli.container" -}}
+- name: cli
+  image: "{{ .Values.global.image.registry }}/{{ tpl .Values.cli.image.repository . }}:{{ tpl .Values.cli.image.tag . }}"
+  {{- include "ozone.containerSecurityContext" . | nindent 2 }}
+  imagePullPolicy: Always
+  command: ["sleep"]
+  args: ["infinity"]
+  volumeMounts:
+    - name: config-volume
+      mountPath: /ozone/etc/hadoop
+    {{- if .Values.fluentBit.enabled }}
+    {{- include "ozone.fluentbit.logsMount" . | nindent 4 }}
+    {{- end }}
+    {{- include "ozone.krb5VolumeMount" . | nindent 4 }}
+  {{- with .Values.cli.resources }}
+  resources:
+    {{- toYaml . | nindent 4 }}
+  {{- end }}
+{{- end -}}
diff --git a/helm-charts/ozone/templates/datanode/statefulset.yaml b/helm-charts/ozone/templates/datanode/statefulset.yaml
index e9458ed..27e0fab 100644
--- a/helm-charts/ozone/templates/datanode/statefulset.yaml
+++ b/helm-charts/ozone/templates/datanode/statefulset.yaml
@@ -60,6 +60,10 @@ spec:
         {{- toYaml . | nindent 8 }}
       {{- end }}
       containers:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "datanode" "techPath" "/var/log/ozone/datanode.log" "auditPath" "/var/log/ozone/dn-audit.log") | nindent 8 }}
+        {{- end }}
+        {{- include "ozone.cli.container" $ | nindent 8 }}
         - name: datanode
           image: "{{ $.Values.global.image.registry }}/{{ tpl $.Values.datanode.image.repository $ }}:{{ tpl $.Values.datanode.image.tag $ }}"
           {{- include "ozone.containerSecurityContext" $ | nindent 10 }}
@@ -79,7 +83,11 @@ spec:
             - name: JAVA_OPTS
               value: "-Djava.net.preferIPv4Stack=true"
             - name: OZONE_OPTS
-              value: "-Djava.net.preferIPv4Stack=true"
+              value: "-Djava.net.preferIPv4Stack=true{{- if $.Values.fluentBit.enabled }} -Dhadoop.log.dir=/var/log/ozone -Dhadoop.log.file=datanode.log -Dlog4j.configurationFile=/ozone/etc/hadoop/log4j2.xml,/ozone/etc/hadoop/dn-audit-log4j2.xml -Dlog4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector{{- end }}"
+            {{- if $.Values.fluentBit.enabled }}
+            - name: HADOOP_LOG_DIR
+              value: /var/log/ozone
+            {{- end }}
             - name: POD_NAME
               valueFrom:
                 fieldRef:
@@ -109,6 +117,9 @@ spec:
           volumeMounts:
             - name: config-volume
               mountPath: /ozone/etc/hadoop
+            {{- if $.Values.fluentBit.enabled }}
+            {{- include "ozone.fluentbit.logsMount" $ | nindent 12 }}
+            {{- end }}
             {{- if eq $.Values.global.storage.mode "storageClass" }}
             - name: data-{{ first $.Values.global.storage.storageClass.datanode.diskIndices }}
               mountPath: /data
@@ -137,6 +148,9 @@ spec:
             {{- toYaml . | nindent 12 }}
           {{- end }}
       volumes:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.volumes" $ | nindent 8 }}
+        {{- end }}
         - name: config-volume
           configMap:
             name: {{ include "ozone.fullname" $ }}-config
diff --git a/helm-charts/ozone/templates/fluentbit/configmap.yaml b/helm-charts/ozone/templates/fluentbit/configmap.yaml
new file mode 100644
index 0000000..a39b247
--- /dev/null
+++ b/helm-charts/ozone/templates/fluentbit/configmap.yaml
@@ -0,0 +1,73 @@
+{{- if .Values.fluentBit.enabled }}
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: fluent-bit-ozone-config
+  namespace: {{ include "ozone.namespace" . }}
+  labels:
+    {{- include "ozone.labels" . | nindent 4 }}
+    app.kubernetes.io/component: fluent-bit-config
+data:
+  fluent-bit.conf: |-
+    [SERVICE]
+        Config_Watch      On
+        Daemon            Off
+        Flush             1
+        HTTP_Listen       0.0.0.0
+        HTTP_Port         {{ .Values.fluentBit.fluentMetricsPort }}
+        HTTP_Server       On
+        Log_Level         info
+
+    [INPUT]
+        Name              tail
+        Tag               tech
+        Path              ${FLUENTBIT_TECH_LOG_PATH}
+        DB                /tmp/fluent-bit-tech.db
+        DB.Sync           Normal
+        Path_Key          source_file
+        Mem_Buf_Limit     5MB
+        Skip_Long_Lines   On
+        Refresh_Interval  10
+
+    [INPUT]
+        Name              tail
+        Tag               audit
+        Path              ${FLUENTBIT_AUDIT_LOG_PATH}
+        DB                /tmp/fluent-bit-audit.db
+        DB.Sync           Normal
+        Path_Key          source_file
+        Mem_Buf_Limit     5MB
+        Skip_Long_Lines   On
+        Refresh_Interval  10
+
+    [FILTER]
+        Name           modify
+        Match          tech
+        Add            app_id {{ .Values.fluentBit.appId | quote }}
+        Add            type_id "Audit"
+        Add            cluster_name ${CLUSTER_NAME}
+        Add            component ${FLUENTBIT_COMPONENT}
+        Add            pod_name ${HOSTNAME}
+        Add            namespace ${POD_NAMESPACE}
+        Rename         log message
+
+    [FILTER]
+        Name           modify
+        Match          audit
+        Add            app_id {{ .Values.fluentBit.appId | quote }}
+        Add            type_id "Audit"
+        Add            cluster_name ${CLUSTER_NAME}
+        Add            component ${FLUENTBIT_COMPONENT}
+        Add            pod_name ${HOSTNAME}
+        Add            namespace ${POD_NAMESPACE}
+        Rename         log message
+
+    [OUTPUT]
+        Name       http
+        Match      *
+        Host       {{ .Values.fluentBit.restlogsHost }}
+        Port       {{ .Values.fluentBit.restlogsPort }}
+        tls        On
+        tls.verify Off
+        Format     json
+{{- end }}
diff --git a/helm-charts/ozone/templates/istio/serviceentry.yaml b/helm-charts/ozone/templates/istio/serviceentry.yaml
index d58d421..fa3d364 100644
--- a/helm-charts/ozone/templates/istio/serviceentry.yaml
+++ b/helm-charts/ozone/templates/istio/serviceentry.yaml
@@ -135,6 +135,27 @@ spec:
 {{- end }}
 {{- end }}
 {{- end }}
+{{- if and .Values.fluentBit.enabled .Values.fluentBit.restlogsHost }}
+---
+apiVersion: networking.istio.io/v1beta1
+kind: ServiceEntry
+metadata:
+  name: {{ include "ozone.fullname" . }}-fluentbit-restlogs
+  namespace: {{ include "ozone.namespace" . }}
+  labels:
+    {{- include "ozone.labels" . | nindent 4 }}
+spec:
+  exportTo:
+    - .
+  hosts:
+    - {{ .Values.fluentBit.restlogsHost | quote }}
+  ports:
+    - name: tls-fluentbit-restlogs
+      number: {{ .Values.fluentBit.restlogsPort }}
+      protocol: TLS
+  resolution: DNS
+  location: MESH_EXTERNAL
+{{- end }}
 ---
 apiVersion: networking.istio.io/v1beta1
 kind: ServiceEntry
diff --git a/helm-charts/ozone/templates/om/statefulset.yaml b/helm-charts/ozone/templates/om/statefulset.yaml
index 3b6396b..813bfaa 100644
--- a/helm-charts/ozone/templates/om/statefulset.yaml
+++ b/helm-charts/ozone/templates/om/statefulset.yaml
@@ -74,6 +74,10 @@ spec:
         {{- toYaml . | nindent 8 }}
       {{- end }}
       containers:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "om" "techPath" "/var/log/ozone/om.log" "auditPath" "/var/log/ozone/om-audit.log") | nindent 8 }}
+        {{- end }}
+        {{- include "ozone.cli.container" $ | nindent 8 }}
         - name: om
           image: "{{ $.Values.global.image.registry }}/{{ tpl $.Values.om.image.repository $ }}:{{ tpl $.Values.om.image.tag $ }}"
           {{- include "ozone.containerSecurityContext" $ | nindent 10 }}
@@ -93,6 +97,12 @@ spec:
               valueFrom:
                 fieldRef:
                   fieldPath: metadata.name
+            {{- if $.Values.fluentBit.enabled }}
+            - name: HADOOP_LOG_DIR
+              value: /var/log/ozone
+            - name: OZONE_OPTS
+              value: "-Dhadoop.log.dir=/var/log/ozone -Dhadoop.log.file=om.log -Dlog4j.configurationFile=/ozone/etc/hadoop/log4j2.xml,/ozone/etc/hadoop/om-audit-log4j2.xml -Dlog4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector"
+            {{- end }}
             {{- if $.Values.global.security.kerberos.enabled }}
             - name: KERBEROS_ENABLED
               value: "true"
@@ -141,6 +151,9 @@ spec:
               mountPath: /ozone/etc/hadoop
             - name: data
               mountPath: /data
+            {{- if $.Values.fluentBit.enabled }}
+            {{- include "ozone.fluentbit.logsMount" $ | nindent 12 }}
+            {{- end }}
             {{- if $.Values.global.security.kerberos.enabled }}
             - name: keytabs
               mountPath: {{ $.Values.global.security.kerberos.keytabDir }}
@@ -161,6 +174,9 @@ spec:
             {{- toYaml . | nindent 12 }}
           {{- end }}
       volumes:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.volumes" $ | nindent 8 }}
+        {{- end }}
         - name: config-volume
           configMap:
             name: {{ include "ozone.fullname" $ }}-config
diff --git a/helm-charts/ozone/templates/recon/deployment.yaml b/helm-charts/ozone/templates/recon/deployment.yaml
index 8121c7d..782409e 100644
--- a/helm-charts/ozone/templates/recon/deployment.yaml
+++ b/helm-charts/ozone/templates/recon/deployment.yaml
@@ -35,6 +35,10 @@ spec:
         {{- toYaml . | nindent 8 }}
       {{- end }}
       containers:
+        {{- if .Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "recon" "techPath" "/var/log/ozone/recon.log" "auditPath" "") | nindent 8 }}
+        {{- end }}
+        {{- include "ozone.cli.container" . | nindent 8 }}
         - name: recon
           image: "{{ .Values.global.image.registry }}/{{ tpl .Values.recon.image.repository . }}:{{ tpl .Values.recon.image.tag . }}"
           {{- include "ozone.containerSecurityContext" $ | nindent 10 }}
@@ -44,8 +48,15 @@ spec:
               name: ui
           command: ["ozone"]
           args: ["recon"]
-          {{- if .Values.global.security.kerberos.enabled }}
+          {{- if or .Values.fluentBit.enabled .Values.global.security.kerberos.enabled }}
           env:
+            {{- if .Values.fluentBit.enabled }}
+            - name: HADOOP_LOG_DIR
+              value: /var/log/ozone
+            - name: OZONE_OPTS
+              value: "-Dhadoop.log.dir=/var/log/ozone -Dhadoop.log.file=recon.log -Dlog4j.configurationFile=/ozone/etc/hadoop/log4j2.xml -Dlog4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector"
+            {{- end }}
+            {{- if .Values.global.security.kerberos.enabled }}
             - name: KERBEROS_ENABLED
               value: "true"
             - name: KERBEROS_PRINCIPAL
@@ -58,10 +69,14 @@ spec:
               value: {{ .Values.global.security.kerberos.wait.timeout | quote }}
             - name: KERBEROS_WAIT_INTERVAL
               value: {{ .Values.global.security.kerberos.wait.interval | quote }}
+            {{- end }}
           {{- end }}
           volumeMounts:
             - name: config-volume
               mountPath: /ozone/etc/hadoop
+            {{- if .Values.fluentBit.enabled }}
+            {{- include "ozone.fluentbit.logsMount" . | nindent 12 }}
+            {{- end }}
             {{- if .Values.global.security.kerberos.enabled }}
             - name: keytabs
               mountPath: {{ .Values.global.security.kerberos.keytabDir }}
@@ -84,6 +99,9 @@ spec:
             {{- toYaml . | nindent 12 }}
           {{- end }}
       volumes:
+        {{- if .Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.volumes" . | nindent 8 }}
+        {{- end }}
         - name: config-volume
           configMap:
             name: {{ include "ozone.fullname" . }}-config
diff --git a/helm-charts/ozone/templates/s3g/deployment.yaml b/helm-charts/ozone/templates/s3g/deployment.yaml
index d97d2a4..fa7f6d3 100644
--- a/helm-charts/ozone/templates/s3g/deployment.yaml
+++ b/helm-charts/ozone/templates/s3g/deployment.yaml
@@ -40,6 +40,10 @@ spec:
         {{- toYaml . | nindent 8 }}
       {{- end }}
       containers:
+        {{- if .Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "s3g" "techPath" "/var/log/ozone/s3g.log" "auditPath" "/var/log/ozone/s3g-audit.log") | nindent 8 }}
+        {{- end }}
+        {{- include "ozone.cli.container" . | nindent 8 }}
         - name: s3g
           image: "{{ .Values.global.image.registry }}/{{ tpl .Values.s3g.image.repository . }}:{{ tpl .Values.s3g.image.tag . }}"
           {{- include "ozone.containerSecurityContext" $ | nindent 10 }}
@@ -49,8 +53,15 @@ spec:
               name: s3
           command: ["ozone"]
           args: ["s3g"]
-          {{- if .Values.global.security.kerberos.enabled }}
+          {{- if or .Values.fluentBit.enabled .Values.global.security.kerberos.enabled }}
           env:
+            {{- if .Values.fluentBit.enabled }}
+            - name: HADOOP_LOG_DIR
+              value: /var/log/ozone
+            - name: OZONE_OPTS
+              value: "-Dhadoop.log.dir=/var/log/ozone -Dhadoop.log.file=s3g.log -Dlog4j.configurationFile=/ozone/etc/hadoop/log4j2.xml,/ozone/etc/hadoop/s3g-audit-log4j2.xml -Dlog4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector"
+            {{- end }}
+            {{- if .Values.global.security.kerberos.enabled }}
             - name: KERBEROS_ENABLED
               value: "true"
             - name: KERBEROS_PRINCIPAL
@@ -63,10 +74,14 @@ spec:
               value: {{ .Values.global.security.kerberos.wait.timeout | quote }}
             - name: KERBEROS_WAIT_INTERVAL
               value: {{ .Values.global.security.kerberos.wait.interval | quote }}
+            {{- end }}
           {{- end }}
           volumeMounts:
             - name: config-volume
               mountPath: /ozone/etc/hadoop
+            {{- if .Values.fluentBit.enabled }}
+            {{- include "ozone.fluentbit.logsMount" . | nindent 12 }}
+            {{- end }}
             {{- if .Values.global.security.kerberos.enabled }}
             - name: keytabs
               mountPath: {{ .Values.global.security.kerberos.keytabDir }}
@@ -89,6 +104,9 @@ spec:
             {{- toYaml . | nindent 12 }}
           {{- end }}
       volumes:
+        {{- if .Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.volumes" . | nindent 8 }}
+        {{- end }}
         - name: config-volume
           configMap:
             name: {{ include "ozone.fullname" . }}-config
diff --git a/helm-charts/ozone/templates/scm/statefulset.yaml b/helm-charts/ozone/templates/scm/statefulset.yaml
index 5511c77..5014ae5 100644
--- a/helm-charts/ozone/templates/scm/statefulset.yaml
+++ b/helm-charts/ozone/templates/scm/statefulset.yaml
@@ -60,6 +60,10 @@ spec:
         {{- toYaml . | nindent 8 }}
       {{- end }}
       containers:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.container" (dict "ctx" $ "component" "scm" "techPath" "/var/log/ozone/scm.log" "auditPath" "/var/log/ozone/scm-audit.log") | nindent 8 }}
+        {{- end }}
+        {{- include "ozone.cli.container" $ | nindent 8 }}
         - name: scm
           image: "{{ $.Values.global.image.registry }}/{{ tpl $.Values.scm.image.repository $ }}:{{ tpl $.Values.scm.image.tag $ }}"
           {{- include "ozone.containerSecurityContext" $ | nindent 10 }}
@@ -85,7 +89,11 @@ spec:
             - name: JAVA_OPTS
               value: "-Djava.net.preferIPv4Stack=true"
             - name: OZONE_OPTS
-              value: "-Djava.net.preferIPv4Stack=true"
+              value: "-Djava.net.preferIPv4Stack=true{{- if $.Values.fluentBit.enabled }} -Dhadoop.log.dir=/var/log/ozone -Dhadoop.log.file=scm.log -Dlog4j.configurationFile=/ozone/etc/hadoop/log4j2.xml,/ozone/etc/hadoop/scm-audit-log4j2.xml -Dlog4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector{{- end }}"
+            {{- if $.Values.fluentBit.enabled }}
+            - name: HADOOP_LOG_DIR
+              value: /var/log/ozone
+            {{- end }}
             - name: POD_NAME
               valueFrom:
                 fieldRef:
@@ -117,6 +125,9 @@ spec:
               mountPath: /ozone/etc/hadoop
             - name: data
               mountPath: /data
+            {{- if $.Values.fluentBit.enabled }}
+            {{- include "ozone.fluentbit.logsMount" $ | nindent 12 }}
+            {{- end }}
             {{- if $.Values.global.security.kerberos.enabled }}
             - name: keytabs
               mountPath: {{ $.Values.global.security.kerberos.keytabDir }}
@@ -132,6 +143,9 @@ spec:
             {{- toYaml . | nindent 12 }}
           {{- end }}
       volumes:
+        {{- if $.Values.fluentBit.enabled }}
+        {{- include "ozone.fluentbit.volumes" $ | nindent 8 }}
+        {{- end }}
         - name: config-volume
           configMap:
             name: {{ include "ozone.fullname" $ }}-config
diff --git a/helm-charts/ozone/values.yaml b/helm-charts/ozone/values.yaml
index 7d90ef9..8bebd6b 100644
--- a/helm-charts/ozone/values.yaml
+++ b/helm-charts/ozone/values.yaml
@@ -101,6 +101,29 @@ secman:
   namespace: ""
   role: ""
 
+# Fluent-bit sidecar — ships /var/log/ozone/<component>.log and <component>-audit.log
+# (where applicable) to the external collector via http+TLS.
+fluentBit:
+  enabled: true
+  image: docker-dev.registry-ci.delta.sbrf.ru/ci03978861/ci03978861/fluent-bit@sha256:e2c58d93e045585c9bb03b429cde86d491e808bf381a9b2c8398db990dfcdabf
+  imagePullPolicy: Always
+  appId: "SPD_OZONE-CI06618727-CI06618727-202410160000000000"
+  # Cluster name shipped on every record (`cluster_name` field). Override per-stand.
+  clusterName: "ozone-dev"
+  # External collector endpoint
+  restlogsHost: "tvlss-log000003.dev.df.sbrf.ru"
+  restlogsPort: 44443
+  # Local fluent-bit metrics endpoint inside the pod
+  fluentMetricsPort: 2200
+  fluentMetricsProtocol: TCP
+  resources:
+    requests:
+      cpu: 20m
+      memory: 128Mi
+    limits:
+      cpu: 100m
+      memory: 256Mi
+
 # Istio service mesh integration
 istio:
   # Set to true to enable all Istio resources (ServiceEntry, Sidecar, PeerAuthentication, AuthorizationPolicy)
@@ -795,17 +818,160 @@ configuration:
       </configuration>
 
     log4j.properties: |-
-      log4j.rootLogger: INFO,stdout
-      log4j.appender.stdout: org.apache.log4j.ConsoleAppender
-      log4j.appender.stdout.layout: org.apache.log4j.PatternLayout
-      log4j.appender.stdout.layout.ConversionPattern: '%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n'
+      log4j.rootLogger=INFO,stdout,RFA
+      log4j.appender.stdout=org.apache.log4j.ConsoleAppender
+      log4j.appender.stdout.layout=org.apache.log4j.PatternLayout
+      log4j.appender.stdout.layout.ConversionPattern=%d{ISO8601} [%t] %p %c: %m%n
+
+      log4j.appender.RFA=org.apache.log4j.RollingFileAppender
+      log4j.appender.RFA.File=${hadoop.log.dir}/${hadoop.log.file}
+      log4j.appender.RFA.MaxFileSize=256MB
+      log4j.appender.RFA.MaxBackupIndex=10
+      log4j.appender.RFA.layout=org.apache.log4j.PatternLayout
+      log4j.appender.RFA.layout.ConversionPattern=%d{ISO8601} [%t] %p %c: %m%n
 
       log4j.logger.org.apache.hadoop.security.ShellBasedUnixGroupsMapping=ERROR
       log4j.logger.org.apache.hadoop.util.NativeCodeLoader=ERROR
 
+    om-audit-log4j2.xml: |-
+      <?xml version="1.0" encoding="UTF-8"?>
+      <Configuration name="PropertiesConfig" monitorInterval="30">
+        <Properties>
+          <Property name="hadoop.log.dir">${sys:hadoop.log.dir:-/var/log/ozone}</Property>
+        </Properties>
+        <Appenders>
+          <RollingFile name="RollingFileOMAudit"
+                       fileName="${hadoop.log.dir}/om-audit.log"
+                       filePattern="${hadoop.log.dir}/om-audit-%d{yyyy-MM-dd-HH-mm-ss}-%i.log.gz">
+            <PatternLayout pattern="%d{DEFAULT} | %-5level | %c{1} | %msg | %throwable{3} %n"/>
+            <Policies>
+              <TimeBasedTriggeringPolicy interval="86400"/>
+              <SizeBasedTriggeringPolicy size="64MB"/>
+            </Policies>
+            <DefaultRolloverStrategy>
+              <Delete basePath="${hadoop.log.dir}" maxDepth="1">
+                <IfFileName glob="om-audit-*.log.gz"/>
+                <IfLastModified age="30d"/>
+              </Delete>
+            </DefaultRolloverStrategy>
+            <Filters>
+              <MarkerFilter marker="READ" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+              <MarkerFilter marker="WRITE" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+            </Filters>
+          </RollingFile>
+        </Appenders>
+        <Loggers>
+          <AsyncLogger name="OMAudit" level="INFO" includeLocation="true" additivity="false">
+            <AppenderRef ref="RollingFileOMAudit"/>
+          </AsyncLogger>
+        </Loggers>
+      </Configuration>
+
+    scm-audit-log4j2.xml: |-
+      <?xml version="1.0" encoding="UTF-8"?>
+      <Configuration name="PropertiesConfig" monitorInterval="30">
+        <Properties>
+          <Property name="hadoop.log.dir">${sys:hadoop.log.dir:-/var/log/ozone}</Property>
+        </Properties>
+        <Appenders>
+          <RollingFile name="RollingFileSCMAudit"
+                       fileName="${hadoop.log.dir}/scm-audit.log"
+                       filePattern="${hadoop.log.dir}/scm-audit-%d{yyyy-MM-dd-HH-mm-ss}-%i.log.gz">
+            <PatternLayout pattern="%d{DEFAULT} | %-5level | %c{1} | %msg | %throwable{3} %n"/>
+            <Policies>
+              <TimeBasedTriggeringPolicy interval="86400"/>
+              <SizeBasedTriggeringPolicy size="64MB"/>
+            </Policies>
+            <DefaultRolloverStrategy>
+              <Delete basePath="${hadoop.log.dir}" maxDepth="1">
+                <IfFileName glob="scm-audit-*.log.gz"/>
+                <IfLastModified age="30d"/>
+              </Delete>
+            </DefaultRolloverStrategy>
+            <Filters>
+              <MarkerFilter marker="READ" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+              <MarkerFilter marker="WRITE" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+            </Filters>
+          </RollingFile>
+        </Appenders>
+        <Loggers>
+          <AsyncLogger name="SCMAudit" level="INFO" includeLocation="true" additivity="false">
+            <AppenderRef ref="RollingFileSCMAudit"/>
+          </AsyncLogger>
+        </Loggers>
+      </Configuration>
+
+    dn-audit-log4j2.xml: |-
+      <?xml version="1.0" encoding="UTF-8"?>
+      <Configuration name="PropertiesConfig" monitorInterval="30">
+        <Properties>
+          <Property name="hadoop.log.dir">${sys:hadoop.log.dir:-/var/log/ozone}</Property>
+        </Properties>
+        <Appenders>
+          <RollingFile name="RollingFileDNAudit"
+                       fileName="${hadoop.log.dir}/dn-audit.log"
+                       filePattern="${hadoop.log.dir}/dn-audit-%d{yyyy-MM-dd-HH-mm-ss}-%i.log.gz">
+            <PatternLayout pattern="%d{DEFAULT} | %-5level | %c{1} | %msg | %throwable{3} %n"/>
+            <Policies>
+              <TimeBasedTriggeringPolicy interval="86400"/>
+              <SizeBasedTriggeringPolicy size="64MB"/>
+            </Policies>
+            <DefaultRolloverStrategy>
+              <Delete basePath="${hadoop.log.dir}" maxDepth="1">
+                <IfFileName glob="dn-audit-*.log.gz"/>
+                <IfLastModified age="30d"/>
+              </Delete>
+            </DefaultRolloverStrategy>
+            <Filters>
+              <MarkerFilter marker="READ" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+              <MarkerFilter marker="WRITE" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+            </Filters>
+          </RollingFile>
+        </Appenders>
+        <Loggers>
+          <AsyncLogger name="DNAudit" level="INFO" includeLocation="true" additivity="false">
+            <AppenderRef ref="RollingFileDNAudit"/>
+          </AsyncLogger>
+        </Loggers>
+      </Configuration>
+
+    s3g-audit-log4j2.xml: |-
+      <?xml version="1.0" encoding="UTF-8"?>
+      <Configuration name="PropertiesConfig" monitorInterval="30">
+        <Properties>
+          <Property name="hadoop.log.dir">${sys:hadoop.log.dir:-/var/log/ozone}</Property>
+        </Properties>
+        <Appenders>
+          <RollingFile name="RollingFileS3GAudit"
+                       fileName="${hadoop.log.dir}/s3g-audit.log"
+                       filePattern="${hadoop.log.dir}/s3g-audit-%d{yyyy-MM-dd-HH-mm-ss}-%i.log.gz">
+            <PatternLayout pattern="%d{DEFAULT} | %-5level | %c{1} | %msg | %throwable{3} %n"/>
+            <Policies>
+              <TimeBasedTriggeringPolicy interval="86400"/>
+              <SizeBasedTriggeringPolicy size="64MB"/>
+            </Policies>
+            <DefaultRolloverStrategy>
+              <Delete basePath="${hadoop.log.dir}" maxDepth="1">
+                <IfFileName glob="s3g-audit-*.log.gz"/>
+                <IfLastModified age="30d"/>
+              </Delete>
+            </DefaultRolloverStrategy>
+            <Filters>
+              <MarkerFilter marker="READ" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+              <MarkerFilter marker="WRITE" onMatch="NEUTRAL" onMismatch="NEUTRAL"/>
+            </Filters>
+          </RollingFile>
+        </Appenders>
+        <Loggers>
+          <AsyncLogger name="S3GAudit" level="INFO" includeLocation="true" additivity="false">
+            <AppenderRef ref="RollingFileS3GAudit"/>
+          </AsyncLogger>
+        </Loggers>
+      </Configuration>
+
     log4j2.xml: |-
       <?xml version="1.0" encoding="UTF-8"?>
-      <Configuration status="WARN">
+      <Configuration monitorInterval="30">
         <Properties>
           <Property name="hadoop.root.logger.level">${sys:hadoop.root.logger.level:-INFO}</Property>
           <Property name="hadoop.root.logger.appender">${sys:hadoop.root.logger.appender:-RFA}</Property>
@@ -834,10 +1000,14 @@ configuration:
               </Delete>
             </DefaultRolloverStrategy>
           </RollingFile>
+          <Async name="Async">
+            <AppenderRef ref="RFA"/>
+          </Async>
         </Appenders>
         <Loggers>
           <Root level="INFO">
-            <AppenderRef ref="${hadoop.root.logger.appender}"/>
+            <AppenderRef ref="console"/>
+            <AppenderRef ref="RFA"/>
           </Root>
           <Logger name="com.amazonaws.http.AmazonHttpClient" level="ERROR"/>
           <Logger name="org.apache.commons.beanutils" level="WARN"/>
@@ -845,6 +1015,11 @@ configuration:
           <Logger name="org.apache.ratis.conf.ConfUtils" level="WARN"/>
           <Logger name="org.apache.hadoop.security.ShellBasedUnixGroupsMapping" level="ERROR"/>
           <Logger name="org.apache.ratis.grpc.client.GrpcClientProtocolClient" level="WARN"/>
+          <Logger name="org.apache.hadoop.ipc.Server">
+            <Filters>
+              <RegexFilter regex=".*Large response size.*" onMatch="DENY" onMismatch="ACCEPT"/>
+            </Filters>
+          </Logger>
         </Loggers>
       </Configuration>
 
